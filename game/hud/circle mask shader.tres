[gd_resource type="CanvasItemShader" format=1]

[resource]

_code = {
"fragment": "uniform float X;
uniform float Y;

//uniform 
vec2 center = vec2(0.5, 0.5); //the center of your circle in screen coords
//uniform 
float radius = 0.25; //the radius of your circle in pixels

//panning
vec2 uv = vec2(UV.x + X, UV.y + Y);

//create the circle
float len = length(UV - center);
//len = smoothstep(radius, radius-blur, len);
   
// Get texture pixel color
vec4 col = tex(TEXTURE, uv);

//masking
float alpha = 1.0;

if (len > radius)
{
	alpha = 0.0;
}
else 
{
	alpha = 1.0;
}

COLOR.rgba = vec4(col.r, col.g, col.b, alpha);",
"fragment_ofs": 0,
"light": "",
"light_ofs": 0,
"vertex": "",
"vertex_ofs": 0
}

